## Typst

``` markdown {#input}
Before the page break.

\newpage

First line on a new page.
```

The `#pagebreak()` command is used for Typst.

``` typst {#expected}
Before the page break.

#pagebreak()

First line on a new page.

```
